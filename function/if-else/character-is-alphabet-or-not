#include<stdio.h>

int check(char);

int main()
{
	char n;
	printf("enter a character\n");
	scanf("%c",&n);

	if(check(n))
		printf("yes its Alphabet\n");
	else
		printf("its not alphabet\n");
	return 0;
}


int check(char c)
{
	if(c>='A'&&c<='Z'||c>='a'&&c<='z')
		return 1;
	else
		return 0;
	
}
